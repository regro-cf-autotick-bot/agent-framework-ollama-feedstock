schema_version: 1

context:
  version: "1.0.0b260730"
  python_min: "3.10"

package:
  name: agent-framework-ollama
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/a/agent-framework-ollama/agent_framework_ollama-${{ version }}.tar.gz
  sha256: 14a048fd0559ebc2fd45597008fe1dde31706339c5e2f674dfd66fa3ffe6eaa7

build:
  noarch: python
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - uv-build
  run:
    - python >=${{ python_min }}
    - agent-framework-core <2,>=1.6.0
    - ollama-python <0.5.4,>=0.5.3

tests:
  - python:
      imports:
        - agent_framework.ollama
      python_version: ${{ python_min }}.*
      pip_check: true

about:
  summary: |
    Ollama integration for Microsoft Agent Framework.
  homepage: https://pypi.org/project/agent-framework-ollama/
  repository: https://github.com/microsoft/agent-framework
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - killua156
    - mgorny
